<% if current_user %>
  <%= form_for [user, decision, comment] do |f| %>
    <div class="form-group">
      <%= f.text_field :body, class: 'form-control', placeholder: "Enter comment" %>
    </div>

    <div class="form-group">
      <%= f.submit "Save", class: 'btn btn-success' %>
    </div>
  <% end %>
<% end %>